apiVersion: v1
kind: Service
metadata:
  name: {{ include "autogen-kernel.fullname" . }}
  labels:
    {{- include "autogen-kernel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autogen-kernel.selectorLabels" . | nindent 4 }}
